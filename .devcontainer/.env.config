# DevContainer 統合設定ファイル
# 実行タイミング：コンテナアタッチ時（毎回）
# 読み込み：attachCommand.sh
#
# このファイルの変更は次回コンテナアタッチ時に反映されます

# ================================================================
# スクリプト処理パラメータ
# ================================================================

# ボリューム永続化対象
# - 初回セットアップ：.storage が空の場合、npm install を実行
# - 以降のアタッチ：シンボリックリンク確認のみ（高速）
# - 開発者の npm install：シンボリックリンク経由で自動的にストレージに反映
# - 動的変更可能：下記を編集して追加削除（コンテナリビルド不要）
VOLUME_TARGETS="
frontend/node_modules
"

# アタッチ後の作業ディレクトリ
START_DIR=frontend

# ================================================================
# アプリケーション環境変数
# ================================================================

# アプリケーション環境
# NODE_ENV=development

# アプリケーション設定（API エンドポイント）
# FRONTEND_URL=http://localhost:3330
# BACKEND_URL=http://localhost:3331
